.env
.env.save
*.backup-*
node_modules/
uploads/
project.tar.gz

# Datos reales / scratch (no subir)
app_state_now.json
backups/
*.NUEVO-*.js

# Datos operativos de las cargas historicas (no se versionan)
carga-historica/registros*.json
